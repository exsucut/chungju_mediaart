.DS_Store
Thumbs.db
source/storyboard_v3/board.html
__pycache__/
.claude/
시민참여_파도_배/
deliver/

# PNG 무손실 마스터 — 레포에 두지 않는다. deliver/ 로 묶어 구글 드라이브로 보낸다.
# 보드가 쓰는 것은 빌드 산출물 orig/ 와 images/ 다.
source/storyboard_v3/images/**/*.png

# 원본은 데스크탑에만 둔다 — 무손실 PNG 라 700MB 가 넘는다.
# 필요하면 make_deliver.py 로 deliver/ 에 묶어 쓴다.
orig/

# 확정안 묶음 — orig/ 에서 골라낸 사본. make_final.py 가 만든다.
final/
deliver/
