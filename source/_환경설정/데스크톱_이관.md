# 데스크톱 이관 체크리스트

노트북(조정웅의 MacBook Pro)에서 데스크톱으로 넘기는 절차.
2026-09-07 기준, 노트북 쪽 준비는 아래 상태로 완료돼 있음.

| 항목 | 노트북 상태 |
|---|---|
| 프로젝트 폴더 161MB | iCloud 동기화 **완료** (스텁 0, 파일 360개) |
| GitHub 저장소 | 커밋 4개 — **1개 미푸시** (`S2 측면부감 읍성 v6 + S3·S4c`) |
| sd25-pe 스킬 | `_환경설정/sd25-pe` 에 사본 포함 |

---

## ⚠️ 노트북에서 먼저 할 일 (딱 하나)

GitHub Desktop → **Push origin**
미푸시 커밋 1개가 남아 있습니다. 이걸 올려야 데스크톱에서 clone만으로 최신을 받습니다.

---

## 데스크톱에서 할 일

### 1. 프로젝트 폴더 (자동)

같은 Apple 계정 로그인 → 시스템 설정 → Apple 계정 → iCloud →
**iCloud Drive → 데스크탑 및 문서 폴더** 켜기.

`Desktop/청주 국가유산 프로잭트` 가 내려옵니다. 161MB라 몇 분 걸립니다.
Finder에서 파일 옆 구름 아이콘이 사라지면 완료.

> 폴더가 보이는데 파일이 안 열리면 아직 다운로드 중입니다.
> 폴더 우클릭 → "지금 다운로드" 로 강제할 수 있습니다.

### 2. 저장소 clone

GitHub Desktop 설치 → GitHub 로그인 →
`File → Clone repository` → `exsucut/chungju_mediaart`
**경로는 기본값 `~/Documents/GitHub/` 그대로.** 빌드 스크립트가 이 경로를 씁니다.

### 3. git 설정 — 빼먹으면 푸시가 끊깁니다

```bash
git config --global credential.helper osxkeychain
git config --global http.postBuffer 524288000
git config --global http.version HTTP/1.1
git config --global http.lowSpeedLimit 0
git config --global http.lowSpeedTime 999999
```

노트북에서 이 설정 없이 푸시했다가 `The remote disconnected` 로 실패했습니다.
이미지가 많아 한 번에 올라가는 양이 커서 기본 버퍼(1MB)로는 안 됩니다.

### 4. Seedance 프롬프트 스킬

```bash
mkdir -p ~/.claude/skills
cp -R "$HOME/Desktop/청주 국가유산 프로잭트/_환경설정/sd25-pe" ~/.claude/skills/
```

### 5. 연결

| 대상 | 방법 |
|---|---|
| **Magnific** | Claude 설정 → Connectors → Add custom connector → `https://mcp.magnific.com` → 브라우저 승인 |
| **Claude in Chrome** | 확장 설치 후 사이드 패널에서 같은 계정 로그인 |

> 안 쓰는 플러그인(sales·marketing·zoom·planetscale 등)은 데스크톱에서 켜지 마세요.
> 노트북에서 Vibe Prospecting 인증 창이 자꾸 뜬 원인입니다. 이 프로젝트는 **Magnific 하나**면 됩니다.

### 6. 동작 확인

```bash
cd "$HOME/Desktop/청주 국가유산 프로잭트/storyboard_v3"
python3 build_board.py      # 아티팩트용 단일 파일
python3 build_web.py        # 웹용 → ~/Documents/GitHub/chungju_mediaart
```

그다음 `배포.command` 더블클릭 → 재빌드 + 커밋 + 푸시.
첫 푸시 때 GitHub 아이디 + Personal Access Token 한 번 입력하면 키체인에 저장됩니다.

---

## 정리해도 되는 것

`storyboard_web/` (34MB) 은 이제 쓰지 않습니다. 내용이 GitHub 저장소와 동일하고
`build_web.py` 로 언제든 다시 만들어집니다. 지우면 iCloud 동기화가 그만큼 빨라집니다.

```bash
rm -rf "$HOME/Desktop/청주 국가유산 프로잭트/storyboard_web"
```

---

## 주소·키

| 항목 | 값 |
|---|---|
| GitHub | `exsucut/chungju_mediaart` |
| 배포 사이트 | https://exsucut.github.io/chungju_mediaart/ |
| 아티팩트 보드 | https://claude.ai/code/artifact/dae4d610-3827-4199-aed2-d3d041a570f9 |
| Supabase URL | `https://iqzwfooylxqhhnbtxegu.supabase.co` |
| Supabase 키 | `index.html` 에 이미 포함 (공개용 publishable) |

---

## 데스크톱에서 Claude에게 먼저 읽힐 문서

이 대화 내용은 넘어가지 않습니다. 새 세션에서 아래를 먼저 읽히세요.

1. `제오경_18샷구성_트랜지션_v3.md` — 25샷 구성·타이밍·트랜지션
2. `storyboard_v3/shots.json` — 샷 데이터 원본
3. `타이포_사양.md` — 최종 합성용 글씨체
4. `reference/*/` 안의 고증 메모 — 962년 기준 시대 필터

## 남은 작업

- **S8b 파도** 톤 일관성 미확인 (다른 홍수 컷은 딥 사파이어로 통일됨)
- **글씨체** 적용 — 샌드박스 제약으로 미적용, `타이포_사양.md` 참고해 최종 합성에서
- Magnific 크레딧 잔여 약 11,000
